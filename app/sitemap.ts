import type { MetadataRoute } from "next";import { programs } from "@/lib/content";
export default function sitemap():MetadataRoute.Sitemap{const base="https://stemeducation.in";const routes=["","/programs","/impact","/presence","/about","/contact","/resources","/resources/case-studies","/resources/knowledge","/resources/faqs","/privacy","/terms","/accessibility"];return [...routes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:url===""?1:.7})),...programs.map(p=>({url:`${base}/programs/${p.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.8}))]}
